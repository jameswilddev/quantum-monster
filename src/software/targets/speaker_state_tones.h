#ifndef QM_TARGET_SPEAKER_STATE_TONES_H

#define QM_TARGET_SPEAKER_STATE_TONES_H

/**
 * @brief The speaker is to produce a square wave at 15625Hz.
 */
#define QM_SPEAKER_STATE_TONE_15625HZ = 4

/**
 * @brief The speaker is to produce a square wave at 12500Hz.
 */
#define QM_SPEAKER_STATE_TONE_12500HZ = 5

/**
 * @brief The speaker is to produce a square wave at 10417Hz.
 */
#define QM_SPEAKER_STATE_TONE_10417HZ = 6

/**
 * @brief The speaker is to produce a square wave at 8929Hz.
 */
#define QM_SPEAKER_STATE_TONE_8929HZ = 7

/**
 * @brief The speaker is to produce a square wave at 7813Hz.
 */
#define QM_SPEAKER_STATE_TONE_7813HZ = 8

/**
 * @brief The speaker is to produce a square wave at 6944Hz.
 */
#define QM_SPEAKER_STATE_TONE_6944HZ = 9

/**
 * @brief The speaker is to produce a square wave at 6250Hz.
 */
#define QM_SPEAKER_STATE_TONE_6250HZ = 10

/**
 * @brief The speaker is to produce a square wave at 5682Hz.
 */
#define QM_SPEAKER_STATE_TONE_5682HZ = 11

/**
 * @brief The speaker is to produce a square wave at 5208Hz.
 */
#define QM_SPEAKER_STATE_TONE_5208HZ = 12

/**
 * @brief The speaker is to produce a square wave at 4808Hz.
 */
#define QM_SPEAKER_STATE_TONE_4808HZ = 13

/**
 * @brief The speaker is to produce a square wave at 4464Hz.
 */
#define QM_SPEAKER_STATE_TONE_4464HZ = 14

/**
 * @brief The speaker is to produce a square wave at 4167Hz.
 */
#define QM_SPEAKER_STATE_TONE_4167HZ = 15

/**
 * @brief The speaker is to produce a square wave at 3906Hz.
 */
#define QM_SPEAKER_STATE_TONE_3906HZ = 16

/**
 * @brief The speaker is to produce a square wave at 3676Hz.
 */
#define QM_SPEAKER_STATE_TONE_3676HZ = 17

/**
 * @brief The speaker is to produce a square wave at 3472Hz.
 */
#define QM_SPEAKER_STATE_TONE_3472HZ = 18

/**
 * @brief The speaker is to produce a square wave at 3289Hz.
 */
#define QM_SPEAKER_STATE_TONE_3289HZ = 19

/**
 * @brief The speaker is to produce a square wave at 3125Hz.
 */
#define QM_SPEAKER_STATE_TONE_3125HZ = 20

/**
 * @brief The speaker is to produce a square wave at 2976Hz.
 */
#define QM_SPEAKER_STATE_TONE_2976HZ = 21

/**
 * @brief The speaker is to produce a square wave at 2841Hz.
 */
#define QM_SPEAKER_STATE_TONE_2841HZ = 22

/**
 * @brief The speaker is to produce a square wave at 2717Hz.
 */
#define QM_SPEAKER_STATE_TONE_2717HZ = 23

/**
 * @brief The speaker is to produce a square wave at 2604Hz.
 */
#define QM_SPEAKER_STATE_TONE_2604HZ = 24

/**
 * @brief The speaker is to produce a square wave at 2500Hz.
 */
#define QM_SPEAKER_STATE_TONE_2500HZ = 25

/**
 * @brief The speaker is to produce a square wave at 2404Hz.
 */
#define QM_SPEAKER_STATE_TONE_2404HZ = 26

/**
 * @brief The speaker is to produce a square wave at 2315Hz.
 */
#define QM_SPEAKER_STATE_TONE_2315HZ = 27

/**
 * @brief The speaker is to produce a square wave at 2232Hz.
 */
#define QM_SPEAKER_STATE_TONE_2232HZ = 28

/**
 * @brief The speaker is to produce a square wave at 2155Hz.
 */
#define QM_SPEAKER_STATE_TONE_2155HZ = 29

/**
 * @brief The speaker is to produce a square wave at 2083Hz.
 */
#define QM_SPEAKER_STATE_TONE_2083HZ = 30

/**
 * @brief The speaker is to produce a square wave at 2016Hz.
 */
#define QM_SPEAKER_STATE_TONE_2016HZ = 31

/**
 * @brief The speaker is to produce a square wave at 1953Hz.
 */
#define QM_SPEAKER_STATE_TONE_1953HZ = 32

/**
 * @brief The speaker is to produce a square wave at 1894Hz.
 */
#define QM_SPEAKER_STATE_TONE_1894HZ = 33

/**
 * @brief The speaker is to produce a square wave at 1838Hz.
 */
#define QM_SPEAKER_STATE_TONE_1838HZ = 34

/**
 * @brief The speaker is to produce a square wave at 1786Hz.
 */
#define QM_SPEAKER_STATE_TONE_1786HZ = 35

/**
 * @brief The speaker is to produce a square wave at 1736Hz.
 */
#define QM_SPEAKER_STATE_TONE_1736HZ = 36

/**
 * @brief The speaker is to produce a square wave at 1689Hz.
 */
#define QM_SPEAKER_STATE_TONE_1689HZ = 37

/**
 * @brief The speaker is to produce a square wave at 1645Hz.
 */
#define QM_SPEAKER_STATE_TONE_1645HZ = 38

/**
 * @brief The speaker is to produce a square wave at 1603Hz.
 */
#define QM_SPEAKER_STATE_TONE_1603HZ = 39

/**
 * @brief The speaker is to produce a square wave at 1563Hz.
 */
#define QM_SPEAKER_STATE_TONE_1563HZ = 40

/**
 * @brief The speaker is to produce a square wave at 1524Hz.
 */
#define QM_SPEAKER_STATE_TONE_1524HZ = 41

/**
 * @brief The speaker is to produce a square wave at 1488Hz.
 */
#define QM_SPEAKER_STATE_TONE_1488HZ = 42

/**
 * @brief The speaker is to produce a square wave at 1453Hz.
 */
#define QM_SPEAKER_STATE_TONE_1453HZ = 43

/**
 * @brief The speaker is to produce a square wave at 1420Hz.
 */
#define QM_SPEAKER_STATE_TONE_1420HZ = 44

/**
 * @brief The speaker is to produce a square wave at 1389Hz.
 */
#define QM_SPEAKER_STATE_TONE_1389HZ = 45

/**
 * @brief The speaker is to produce a square wave at 1359Hz.
 */
#define QM_SPEAKER_STATE_TONE_1359HZ = 46

/**
 * @brief The speaker is to produce a square wave at 1330Hz.
 */
#define QM_SPEAKER_STATE_TONE_1330HZ = 47

/**
 * @brief The speaker is to produce a square wave at 1302Hz.
 */
#define QM_SPEAKER_STATE_TONE_1302HZ = 48

/**
 * @brief The speaker is to produce a square wave at 1276Hz.
 */
#define QM_SPEAKER_STATE_TONE_1276HZ = 49

/**
 * @brief The speaker is to produce a square wave at 1250Hz.
 */
#define QM_SPEAKER_STATE_TONE_1250HZ = 50

/**
 * @brief The speaker is to produce a square wave at 1225Hz.
 */
#define QM_SPEAKER_STATE_TONE_1225HZ = 51

/**
 * @brief The speaker is to produce a square wave at 1202Hz.
 */
#define QM_SPEAKER_STATE_TONE_1202HZ = 52

/**
 * @brief The speaker is to produce a square wave at 1179Hz.
 */
#define QM_SPEAKER_STATE_TONE_1179HZ = 53

/**
 * @brief The speaker is to produce a square wave at 1157Hz.
 */
#define QM_SPEAKER_STATE_TONE_1157HZ = 54

/**
 * @brief The speaker is to produce a square wave at 1136Hz.
 */
#define QM_SPEAKER_STATE_TONE_1136HZ = 55

/**
 * @brief The speaker is to produce a square wave at 1116Hz.
 */
#define QM_SPEAKER_STATE_TONE_1116HZ = 56

/**
 * @brief The speaker is to produce a square wave at 1096Hz.
 */
#define QM_SPEAKER_STATE_TONE_1096HZ = 57

/**
 * @brief The speaker is to produce a square wave at 1078Hz.
 */
#define QM_SPEAKER_STATE_TONE_1078HZ = 58

/**
 * @brief The speaker is to produce a square wave at 1059Hz.
 */
#define QM_SPEAKER_STATE_TONE_1059HZ = 59

/**
 * @brief The speaker is to produce a square wave at 1042Hz.
 */
#define QM_SPEAKER_STATE_TONE_1042HZ = 60

/**
 * @brief The speaker is to produce a square wave at 1025Hz.
 */
#define QM_SPEAKER_STATE_TONE_1025HZ = 61

/**
 * @brief The speaker is to produce a square wave at 1008Hz.
 */
#define QM_SPEAKER_STATE_TONE_1008HZ = 62

/**
 * @brief The speaker is to produce a square wave at 992Hz.
 */
#define QM_SPEAKER_STATE_TONE_992HZ = 63

/**
 * @brief The speaker is to produce a square wave at 977Hz.
 */
#define QM_SPEAKER_STATE_TONE_977HZ = 64

/**
 * @brief The speaker is to produce a square wave at 962Hz.
 */
#define QM_SPEAKER_STATE_TONE_962HZ = 65

/**
 * @brief The speaker is to produce a square wave at 947Hz.
 */
#define QM_SPEAKER_STATE_TONE_947HZ = 66

/**
 * @brief The speaker is to produce a square wave at 933Hz.
 */
#define QM_SPEAKER_STATE_TONE_933HZ = 67

/**
 * @brief The speaker is to produce a square wave at 919Hz.
 */
#define QM_SPEAKER_STATE_TONE_919HZ = 68

/**
 * @brief The speaker is to produce a square wave at 906Hz.
 */
#define QM_SPEAKER_STATE_TONE_906HZ = 69

/**
 * @brief The speaker is to produce a square wave at 893Hz.
 */
#define QM_SPEAKER_STATE_TONE_893HZ = 70

/**
 * @brief The speaker is to produce a square wave at 880Hz.
 */
#define QM_SPEAKER_STATE_TONE_880HZ = 71

/**
 * @brief The speaker is to produce a square wave at 868Hz.
 */
#define QM_SPEAKER_STATE_TONE_868HZ = 72

/**
 * @brief The speaker is to produce a square wave at 856Hz.
 */
#define QM_SPEAKER_STATE_TONE_856HZ = 73

/**
 * @brief The speaker is to produce a square wave at 845Hz.
 */
#define QM_SPEAKER_STATE_TONE_845HZ = 74

/**
 * @brief The speaker is to produce a square wave at 833Hz.
 */
#define QM_SPEAKER_STATE_TONE_833HZ = 75

/**
 * @brief The speaker is to produce a square wave at 822Hz.
 */
#define QM_SPEAKER_STATE_TONE_822HZ = 76

/**
 * @brief The speaker is to produce a square wave at 812Hz.
 */
#define QM_SPEAKER_STATE_TONE_812HZ = 77

/**
 * @brief The speaker is to produce a square wave at 801Hz.
 */
#define QM_SPEAKER_STATE_TONE_801HZ = 78

/**
 * @brief The speaker is to produce a square wave at 791Hz.
 */
#define QM_SPEAKER_STATE_TONE_791HZ = 79

/**
 * @brief The speaker is to produce a square wave at 781Hz.
 */
#define QM_SPEAKER_STATE_TONE_781HZ = 80

/**
 * @brief The speaker is to produce a square wave at 772Hz.
 */
#define QM_SPEAKER_STATE_TONE_772HZ = 81

/**
 * @brief The speaker is to produce a square wave at 762Hz.
 */
#define QM_SPEAKER_STATE_TONE_762HZ = 82

/**
 * @brief The speaker is to produce a square wave at 753Hz.
 */
#define QM_SPEAKER_STATE_TONE_753HZ = 83

/**
 * @brief The speaker is to produce a square wave at 744Hz.
 */
#define QM_SPEAKER_STATE_TONE_744HZ = 84

/**
 * @brief The speaker is to produce a square wave at 735Hz.
 */
#define QM_SPEAKER_STATE_TONE_735HZ = 85

/**
 * @brief The speaker is to produce a square wave at 727Hz.
 */
#define QM_SPEAKER_STATE_TONE_727HZ = 86

/**
 * @brief The speaker is to produce a square wave at 718Hz.
 */
#define QM_SPEAKER_STATE_TONE_718HZ = 87

/**
 * @brief The speaker is to produce a square wave at 710Hz.
 */
#define QM_SPEAKER_STATE_TONE_710HZ = 88

/**
 * @brief The speaker is to produce a square wave at 702Hz.
 */
#define QM_SPEAKER_STATE_TONE_702HZ = 89

/**
 * @brief The speaker is to produce a square wave at 694Hz.
 */
#define QM_SPEAKER_STATE_TONE_694HZ = 90

/**
 * @brief The speaker is to produce a square wave at 687Hz.
 */
#define QM_SPEAKER_STATE_TONE_687HZ = 91

/**
 * @brief The speaker is to produce a square wave at 679Hz.
 */
#define QM_SPEAKER_STATE_TONE_679HZ = 92

/**
 * @brief The speaker is to produce a square wave at 672Hz.
 */
#define QM_SPEAKER_STATE_TONE_672HZ = 93

/**
 * @brief The speaker is to produce a square wave at 665Hz.
 */
#define QM_SPEAKER_STATE_TONE_665HZ = 94

/**
 * @brief The speaker is to produce a square wave at 658Hz.
 */
#define QM_SPEAKER_STATE_TONE_658HZ = 95

/**
 * @brief The speaker is to produce a square wave at 651Hz.
 */
#define QM_SPEAKER_STATE_TONE_651HZ = 96

/**
 * @brief The speaker is to produce a square wave at 644Hz.
 */
#define QM_SPEAKER_STATE_TONE_644HZ = 97

/**
 * @brief The speaker is to produce a square wave at 638Hz.
 */
#define QM_SPEAKER_STATE_TONE_638HZ = 98

/**
 * @brief The speaker is to produce a square wave at 631Hz.
 */
#define QM_SPEAKER_STATE_TONE_631HZ = 99

/**
 * @brief The speaker is to produce a square wave at 625Hz.
 */
#define QM_SPEAKER_STATE_TONE_625HZ = 100

/**
 * @brief The speaker is to produce a square wave at 619Hz.
 */
#define QM_SPEAKER_STATE_TONE_619HZ = 101

/**
 * @brief The speaker is to produce a square wave at 613Hz.
 */
#define QM_SPEAKER_STATE_TONE_613HZ = 102

/**
 * @brief The speaker is to produce a square wave at 607Hz.
 */
#define QM_SPEAKER_STATE_TONE_607HZ = 103

/**
 * @brief The speaker is to produce a square wave at 601Hz.
 */
#define QM_SPEAKER_STATE_TONE_601HZ = 104

/**
 * @brief The speaker is to produce a square wave at 595Hz.
 */
#define QM_SPEAKER_STATE_TONE_595HZ = 105

/**
 * @brief The speaker is to produce a square wave at 590Hz.
 */
#define QM_SPEAKER_STATE_TONE_590HZ = 106

/**
 * @brief The speaker is to produce a square wave at 584Hz.
 */
#define QM_SPEAKER_STATE_TONE_584HZ = 107

/**
 * @brief The speaker is to produce a square wave at 579Hz.
 */
#define QM_SPEAKER_STATE_TONE_579HZ = 108

/**
 * @brief The speaker is to produce a square wave at 573Hz.
 */
#define QM_SPEAKER_STATE_TONE_573HZ = 109

/**
 * @brief The speaker is to produce a square wave at 568Hz.
 */
#define QM_SPEAKER_STATE_TONE_568HZ = 110

/**
 * @brief The speaker is to produce a square wave at 563Hz.
 */
#define QM_SPEAKER_STATE_TONE_563HZ = 111

/**
 * @brief The speaker is to produce a square wave at 558Hz.
 */
#define QM_SPEAKER_STATE_TONE_558HZ = 112

/**
 * @brief The speaker is to produce a square wave at 553Hz.
 */
#define QM_SPEAKER_STATE_TONE_553HZ = 113

/**
 * @brief The speaker is to produce a square wave at 548Hz.
 */
#define QM_SPEAKER_STATE_TONE_548HZ = 114

/**
 * @brief The speaker is to produce a square wave at 543Hz.
 */
#define QM_SPEAKER_STATE_TONE_543HZ = 115

/**
 * @brief The speaker is to produce a square wave at 539Hz.
 */
#define QM_SPEAKER_STATE_TONE_539HZ = 116

/**
 * @brief The speaker is to produce a square wave at 534Hz.
 */
#define QM_SPEAKER_STATE_TONE_534HZ = 117

/**
 * @brief The speaker is to produce a square wave at 530Hz.
 */
#define QM_SPEAKER_STATE_TONE_530HZ = 118

/**
 * @brief The speaker is to produce a square wave at 525Hz.
 */
#define QM_SPEAKER_STATE_TONE_525HZ = 119

/**
 * @brief The speaker is to produce a square wave at 521Hz.
 */
#define QM_SPEAKER_STATE_TONE_521HZ = 120

/**
 * @brief The speaker is to produce a square wave at 517Hz.
 */
#define QM_SPEAKER_STATE_TONE_517HZ = 121

/**
 * @brief The speaker is to produce a square wave at 512Hz.
 */
#define QM_SPEAKER_STATE_TONE_512HZ = 122

/**
 * @brief The speaker is to produce a square wave at 508Hz.
 */
#define QM_SPEAKER_STATE_TONE_508HZ = 123

/**
 * @brief The speaker is to produce a square wave at 504Hz.
 */
#define QM_SPEAKER_STATE_TONE_504HZ = 124

/**
 * @brief The speaker is to produce a square wave at 500Hz.
 */
#define QM_SPEAKER_STATE_TONE_500HZ = 125

/**
 * @brief The speaker is to produce a square wave at 496Hz.
 */
#define QM_SPEAKER_STATE_TONE_496HZ = 126

/**
 * @brief The speaker is to produce a square wave at 492Hz.
 */
#define QM_SPEAKER_STATE_TONE_492HZ = 127

/**
 * @brief The speaker is to produce a square wave at 488Hz.
 */
#define QM_SPEAKER_STATE_TONE_488HZ = 128

/**
 * @brief The speaker is to produce a square wave at 484Hz.
 */
#define QM_SPEAKER_STATE_TONE_484HZ = 129

/**
 * @brief The speaker is to produce a square wave at 481Hz.
 */
#define QM_SPEAKER_STATE_TONE_481HZ = 130

/**
 * @brief The speaker is to produce a square wave at 477Hz.
 */
#define QM_SPEAKER_STATE_TONE_477HZ = 131

/**
 * @brief The speaker is to produce a square wave at 473Hz.
 */
#define QM_SPEAKER_STATE_TONE_473HZ = 132

/**
 * @brief The speaker is to produce a square wave at 470Hz.
 */
#define QM_SPEAKER_STATE_TONE_470HZ = 133

/**
 * @brief The speaker is to produce a square wave at 466Hz.
 */
#define QM_SPEAKER_STATE_TONE_466HZ = 134

/**
 * @brief The speaker is to produce a square wave at 463Hz.
 */
#define QM_SPEAKER_STATE_TONE_463HZ = 135

/**
 * @brief The speaker is to produce a square wave at 460Hz.
 */
#define QM_SPEAKER_STATE_TONE_460HZ = 136

/**
 * @brief The speaker is to produce a square wave at 456Hz.
 */
#define QM_SPEAKER_STATE_TONE_456HZ = 137

/**
 * @brief The speaker is to produce a square wave at 453Hz.
 */
#define QM_SPEAKER_STATE_TONE_453HZ = 138

/**
 * @brief The speaker is to produce a square wave at 450Hz.
 */
#define QM_SPEAKER_STATE_TONE_450HZ = 139

/**
 * @brief The speaker is to produce a square wave at 446Hz.
 */
#define QM_SPEAKER_STATE_TONE_446HZ = 140

/**
 * @brief The speaker is to produce a square wave at 443Hz.
 */
#define QM_SPEAKER_STATE_TONE_443HZ = 141

/**
 * @brief The speaker is to produce a square wave at 440Hz.
 */
#define QM_SPEAKER_STATE_TONE_440HZ = 142

/**
 * @brief The speaker is to produce a square wave at 437Hz.
 */
#define QM_SPEAKER_STATE_TONE_437HZ = 143

/**
 * @brief The speaker is to produce a square wave at 434Hz.
 */
#define QM_SPEAKER_STATE_TONE_434HZ = 144

/**
 * @brief The speaker is to produce a square wave at 431Hz.
 */
#define QM_SPEAKER_STATE_TONE_431HZ = 145

/**
 * @brief The speaker is to produce a square wave at 428Hz.
 */
#define QM_SPEAKER_STATE_TONE_428HZ = 146

/**
 * @brief The speaker is to produce a square wave at 425Hz.
 */
#define QM_SPEAKER_STATE_TONE_425HZ = 147

/**
 * @brief The speaker is to produce a square wave at 422Hz.
 */
#define QM_SPEAKER_STATE_TONE_422HZ = 148

/**
 * @brief The speaker is to produce a square wave at 419Hz.
 */
#define QM_SPEAKER_STATE_TONE_419HZ = 149

/**
 * @brief The speaker is to produce a square wave at 417Hz.
 */
#define QM_SPEAKER_STATE_TONE_417HZ = 150

/**
 * @brief The speaker is to produce a square wave at 414Hz.
 */
#define QM_SPEAKER_STATE_TONE_414HZ = 151

/**
 * @brief The speaker is to produce a square wave at 411Hz.
 */
#define QM_SPEAKER_STATE_TONE_411HZ = 152

/**
 * @brief The speaker is to produce a square wave at 408Hz.
 */
#define QM_SPEAKER_STATE_TONE_408HZ = 153

/**
 * @brief The speaker is to produce a square wave at 406Hz.
 */
#define QM_SPEAKER_STATE_TONE_406HZ = 154

/**
 * @brief The speaker is to produce a square wave at 403Hz.
 */
#define QM_SPEAKER_STATE_TONE_403HZ = 155

/**
 * @brief The speaker is to produce a square wave at 401Hz.
 */
#define QM_SPEAKER_STATE_TONE_401HZ = 156

/**
 * @brief The speaker is to produce a square wave at 398Hz.
 */
#define QM_SPEAKER_STATE_TONE_398HZ = 157

/**
 * @brief The speaker is to produce a square wave at 396Hz.
 */
#define QM_SPEAKER_STATE_TONE_396HZ = 158

/**
 * @brief The speaker is to produce a square wave at 393Hz.
 */
#define QM_SPEAKER_STATE_TONE_393HZ = 159

/**
 * @brief The speaker is to produce a square wave at 391Hz.
 */
#define QM_SPEAKER_STATE_TONE_391HZ = 160

/**
 * @brief The speaker is to produce a square wave at 388Hz.
 */
#define QM_SPEAKER_STATE_TONE_388HZ = 161

/**
 * @brief The speaker is to produce a square wave at 386Hz.
 */
#define QM_SPEAKER_STATE_TONE_386HZ = 162

/**
 * @brief The speaker is to produce a square wave at 383Hz.
 */
#define QM_SPEAKER_STATE_TONE_383HZ = 163

/**
 * @brief The speaker is to produce a square wave at 381Hz.
 */
#define QM_SPEAKER_STATE_TONE_381HZ = 164

/**
 * @brief The speaker is to produce a square wave at 379Hz.
 */
#define QM_SPEAKER_STATE_TONE_379HZ = 165

/**
 * @brief The speaker is to produce a square wave at 377Hz.
 */
#define QM_SPEAKER_STATE_TONE_377HZ = 166

/**
 * @brief The speaker is to produce a square wave at 374Hz.
 */
#define QM_SPEAKER_STATE_TONE_374HZ = 167

/**
 * @brief The speaker is to produce a square wave at 372Hz.
 */
#define QM_SPEAKER_STATE_TONE_372HZ = 168

/**
 * @brief The speaker is to produce a square wave at 370Hz.
 */
#define QM_SPEAKER_STATE_TONE_370HZ = 169

/**
 * @brief The speaker is to produce a square wave at 368Hz.
 */
#define QM_SPEAKER_STATE_TONE_368HZ = 170

/**
 * @brief The speaker is to produce a square wave at 365Hz.
 */
#define QM_SPEAKER_STATE_TONE_365HZ = 171

/**
 * @brief The speaker is to produce a square wave at 363Hz.
 */
#define QM_SPEAKER_STATE_TONE_363HZ = 172

/**
 * @brief The speaker is to produce a square wave at 361Hz.
 */
#define QM_SPEAKER_STATE_TONE_361HZ = 173

/**
 * @brief The speaker is to produce a square wave at 359Hz.
 */
#define QM_SPEAKER_STATE_TONE_359HZ = 174

/**
 * @brief The speaker is to produce a square wave at 357Hz.
 */
#define QM_SPEAKER_STATE_TONE_357HZ = 175

/**
 * @brief The speaker is to produce a square wave at 355Hz.
 */
#define QM_SPEAKER_STATE_TONE_355HZ = 176

/**
 * @brief The speaker is to produce a square wave at 353Hz.
 */
#define QM_SPEAKER_STATE_TONE_353HZ = 177

/**
 * @brief The speaker is to produce a square wave at 351Hz.
 */
#define QM_SPEAKER_STATE_TONE_351HZ = 178

/**
 * @brief The speaker is to produce a square wave at 349Hz.
 */
#define QM_SPEAKER_STATE_TONE_349HZ = 179

/**
 * @brief The speaker is to produce a square wave at 347Hz.
 */
#define QM_SPEAKER_STATE_TONE_347HZ = 180

/**
 * @brief The speaker is to produce a square wave at 345Hz.
 */
#define QM_SPEAKER_STATE_TONE_345HZ = 181

/**
 * @brief The speaker is to produce a square wave at 343Hz.
 */
#define QM_SPEAKER_STATE_TONE_343HZ = 182

/**
 * @brief The speaker is to produce a square wave at 342Hz.
 */
#define QM_SPEAKER_STATE_TONE_342HZ = 183

/**
 * @brief The speaker is to produce a square wave at 340Hz.
 */
#define QM_SPEAKER_STATE_TONE_340HZ = 184

/**
 * @brief The speaker is to produce a square wave at 338Hz.
 */
#define QM_SPEAKER_STATE_TONE_338HZ = 185

/**
 * @brief The speaker is to produce a square wave at 336Hz.
 */
#define QM_SPEAKER_STATE_TONE_336HZ = 186

/**
 * @brief The speaker is to produce a square wave at 334Hz.
 */
#define QM_SPEAKER_STATE_TONE_334HZ = 187

/**
 * @brief The speaker is to produce a square wave at 332Hz.
 */
#define QM_SPEAKER_STATE_TONE_332HZ = 188

/**
 * @brief The speaker is to produce a square wave at 331Hz.
 */
#define QM_SPEAKER_STATE_TONE_331HZ = 189

/**
 * @brief The speaker is to produce a square wave at 329Hz.
 */
#define QM_SPEAKER_STATE_TONE_329HZ = 190

/**
 * @brief The speaker is to produce a square wave at 327Hz.
 */
#define QM_SPEAKER_STATE_TONE_327HZ = 191

/**
 * @brief The speaker is to produce a square wave at 326Hz.
 */
#define QM_SPEAKER_STATE_TONE_326HZ = 192

/**
 * @brief The speaker is to produce a square wave at 324Hz.
 */
#define QM_SPEAKER_STATE_TONE_324HZ = 193

/**
 * @brief The speaker is to produce a square wave at 322Hz.
 */
#define QM_SPEAKER_STATE_TONE_322HZ = 194

/**
 * @brief The speaker is to produce a square wave at 321Hz.
 */
#define QM_SPEAKER_STATE_TONE_321HZ = 195

/**
 * @brief The speaker is to produce a square wave at 319Hz.
 */
#define QM_SPEAKER_STATE_TONE_319HZ = 196

/**
 * @brief The speaker is to produce a square wave at 317Hz.
 */
#define QM_SPEAKER_STATE_TONE_317HZ = 197

/**
 * @brief The speaker is to produce a square wave at 316Hz.
 */
#define QM_SPEAKER_STATE_TONE_316HZ = 198

/**
 * @brief The speaker is to produce a square wave at 314Hz.
 */
#define QM_SPEAKER_STATE_TONE_314HZ = 199

/**
 * @brief The speaker is to produce a square wave at 313Hz.
 */
#define QM_SPEAKER_STATE_TONE_313HZ = 200

/**
 * @brief The speaker is to produce a square wave at 311Hz.
 */
#define QM_SPEAKER_STATE_TONE_311HZ = 201

/**
 * @brief The speaker is to produce a square wave at 309Hz.
 */
#define QM_SPEAKER_STATE_TONE_309HZ = 202

/**
 * @brief The speaker is to produce a square wave at 308Hz.
 */
#define QM_SPEAKER_STATE_TONE_308HZ = 203

/**
 * @brief The speaker is to produce a square wave at 306Hz.
 */
#define QM_SPEAKER_STATE_TONE_306HZ = 204

/**
 * @brief The speaker is to produce a square wave at 305Hz.
 */
#define QM_SPEAKER_STATE_TONE_305HZ = 205

/**
 * @brief The speaker is to produce a square wave at 303Hz.
 */
#define QM_SPEAKER_STATE_TONE_303HZ = 206

/**
 * @brief The speaker is to produce a square wave at 302Hz.
 */
#define QM_SPEAKER_STATE_TONE_302HZ = 207

/**
 * @brief The speaker is to produce a square wave at 300Hz.
 */
#define QM_SPEAKER_STATE_TONE_300HZ = 208

/**
 * @brief The speaker is to produce a square wave at 299Hz.
 */
#define QM_SPEAKER_STATE_TONE_299HZ = 209

/**
 * @brief The speaker is to produce a square wave at 298Hz.
 */
#define QM_SPEAKER_STATE_TONE_298HZ = 210

/**
 * @brief The speaker is to produce a square wave at 296Hz.
 */
#define QM_SPEAKER_STATE_TONE_296HZ = 211

/**
 * @brief The speaker is to produce a square wave at 295Hz.
 */
#define QM_SPEAKER_STATE_TONE_295HZ = 212

/**
 * @brief The speaker is to produce a square wave at 293Hz.
 */
#define QM_SPEAKER_STATE_TONE_293HZ = 213

/**
 * @brief The speaker is to produce a square wave at 292Hz.
 */
#define QM_SPEAKER_STATE_TONE_292HZ = 214

/**
 * @brief The speaker is to produce a square wave at 291Hz.
 */
#define QM_SPEAKER_STATE_TONE_291HZ = 215

/**
 * @brief The speaker is to produce a square wave at 289Hz.
 */
#define QM_SPEAKER_STATE_TONE_289HZ = 216

/**
 * @brief The speaker is to produce a square wave at 288Hz.
 */
#define QM_SPEAKER_STATE_TONE_288HZ = 217

/**
 * @brief The speaker is to produce a square wave at 287Hz.
 */
#define QM_SPEAKER_STATE_TONE_287HZ = 218

/**
 * @brief The speaker is to produce a square wave at 285Hz.
 */
#define QM_SPEAKER_STATE_TONE_285HZ = 219

/**
 * @brief The speaker is to produce a square wave at 284Hz.
 */
#define QM_SPEAKER_STATE_TONE_284HZ = 220

/**
 * @brief The speaker is to produce a square wave at 283Hz.
 */
#define QM_SPEAKER_STATE_TONE_283HZ = 221

/**
 * @brief The speaker is to produce a square wave at 282Hz.
 */
#define QM_SPEAKER_STATE_TONE_282HZ = 222

/**
 * @brief The speaker is to produce a square wave at 280Hz.
 */
#define QM_SPEAKER_STATE_TONE_280HZ = 223

/**
 * @brief The speaker is to produce a square wave at 279Hz.
 */
#define QM_SPEAKER_STATE_TONE_279HZ = 224

/**
 * @brief The speaker is to produce a square wave at 278Hz.
 */
#define QM_SPEAKER_STATE_TONE_278HZ = 225

/**
 * @brief The speaker is to produce a square wave at 277Hz.
 */
#define QM_SPEAKER_STATE_TONE_277HZ = 226

/**
 * @brief The speaker is to produce a square wave at 275Hz.
 */
#define QM_SPEAKER_STATE_TONE_275HZ = 227

/**
 * @brief The speaker is to produce a square wave at 274Hz.
 */
#define QM_SPEAKER_STATE_TONE_274HZ = 228

/**
 * @brief The speaker is to produce a square wave at 273Hz.
 */
#define QM_SPEAKER_STATE_TONE_273HZ = 229

/**
 * @brief The speaker is to produce a square wave at 272Hz.
 */
#define QM_SPEAKER_STATE_TONE_272HZ = 230

/**
 * @brief The speaker is to produce a square wave at 271Hz.
 */
#define QM_SPEAKER_STATE_TONE_271HZ = 231

/**
 * @brief The speaker is to produce a square wave at 269Hz.
 */
#define QM_SPEAKER_STATE_TONE_269HZ = 232

/**
 * @brief The speaker is to produce a square wave at 268Hz.
 */
#define QM_SPEAKER_STATE_TONE_268HZ = 233

/**
 * @brief The speaker is to produce a square wave at 267Hz.
 */
#define QM_SPEAKER_STATE_TONE_267HZ = 234

/**
 * @brief The speaker is to produce a square wave at 266Hz.
 */
#define QM_SPEAKER_STATE_TONE_266HZ = 235

/**
 * @brief The speaker is to produce a square wave at 265Hz.
 */
#define QM_SPEAKER_STATE_TONE_265HZ = 236

/**
 * @brief The speaker is to produce a square wave at 264Hz.
 */
#define QM_SPEAKER_STATE_TONE_264HZ = 237

/**
 * @brief The speaker is to produce a square wave at 263Hz.
 */
#define QM_SPEAKER_STATE_TONE_263HZ = 238

/**
 * @brief The speaker is to produce a square wave at 262Hz.
 */
#define QM_SPEAKER_STATE_TONE_262HZ = 239

/**
 * @brief The speaker is to produce a square wave at 260Hz.
 */
#define QM_SPEAKER_STATE_TONE_260HZ = 240

/**
 * @brief The speaker is to produce a square wave at 259Hz.
 */
#define QM_SPEAKER_STATE_TONE_259HZ = 241

/**
 * @brief The speaker is to produce a square wave at 258Hz.
 */
#define QM_SPEAKER_STATE_TONE_258HZ = 242

/**
 * @brief The speaker is to produce a square wave at 257Hz.
 */
#define QM_SPEAKER_STATE_TONE_257HZ = 243

/**
 * @brief The speaker is to produce a square wave at 256Hz.
 */
#define QM_SPEAKER_STATE_TONE_256HZ = 244

/**
 * @brief The speaker is to produce a square wave at 255Hz.
 */
#define QM_SPEAKER_STATE_TONE_255HZ = 245

/**
 * @brief The speaker is to produce a square wave at 254Hz.
 */
#define QM_SPEAKER_STATE_TONE_254HZ = 246

/**
 * @brief The speaker is to produce a square wave at 253Hz.
 */
#define QM_SPEAKER_STATE_TONE_253HZ = 247

/**
 * @brief The speaker is to produce a square wave at 252Hz.
 */
#define QM_SPEAKER_STATE_TONE_252HZ = 248

/**
 * @brief The speaker is to produce a square wave at 251Hz.
 */
#define QM_SPEAKER_STATE_TONE_251HZ = 249

/**
 * @brief The speaker is to produce a square wave at 250Hz.
 */
#define QM_SPEAKER_STATE_TONE_250HZ = 250

/**
 * @brief The speaker is to produce a square wave at 249Hz.
 */
#define QM_SPEAKER_STATE_TONE_249HZ = 251

/**
 * @brief The speaker is to produce a square wave at 248Hz.
 */
#define QM_SPEAKER_STATE_TONE_248HZ = 252

/**
 * @brief The speaker is to produce a square wave at 247Hz.
 */
#define QM_SPEAKER_STATE_TONE_247HZ = 253

/**
 * @brief The speaker is to produce a square wave at 246Hz.
 */
#define QM_SPEAKER_STATE_TONE_246HZ = 254

/**
 * @brief The speaker is to produce a square wave at 245Hz.
 */
#define QM_SPEAKER_STATE_TONE_245HZ = 255

#endif
